# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environments (uv.lock IS committed for reproducibility)
.venv/

# Data (PTB-XL and any source corpus — never committed). Anchored to repo root
# so it does not match the src/deep_ecg/data/ package.
/data/

# Experiment outputs
outputs/
multirun/
wandb/
checkpoints/
*.pt
*.ckpt

# Exported serving bundles (models are large; rebuild with scripts/export_model.py)
artifacts/
*.onnx
*.ts

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/

# Workspace convention: project tooling and private notes never leave the machine
.claude/
